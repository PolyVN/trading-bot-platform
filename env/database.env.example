# =============================================================================
# Database (MongoDB + Redis)
# =============================================================================

# === MongoDB ===
MONGO_USER=admin
MONGO_PASS=

# === Redis ===
REDIS_PASSWORD=

# === Redis Sentinel (production HA only) ===
# Only needed when using docker-compose.ha.yml overlay
# Used for replica-announce-ip
DB_HOST=10.0.0.3
