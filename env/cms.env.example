# =============================================================================
# CMS (Backend + Frontend + Grafana)
# =============================================================================

# === Database Server ===
DB_HOST=10.0.0.3
MONGO_USER=admin
MONGO_PASS=
REDIS_PASSWORD=
# Full URI constructed in docker-compose.yml:
# mongodb://${MONGO_USER}:${MONGO_PASS}@${DB_HOST}:27017/trading

# === Auth ===
# Generate with: openssl rand -hex 32
JWT_SECRET=
NEXTAUTH_SECRET=
# Public URL where CMS is accessible (used by NextAuth for callbacks)
CMS_PUBLIC_URL=https://cms.yourdomain.com

# === Encryption ===
# For encrypting/decrypting wallet credentials in MongoDB
# MUST match Trading Engine key
ENCRYPTION_KEY=

# === Grafana ===
GRAFANA_PASSWORD=admin
