# =============================================================================
# Trading Engine — Infrastructure Config
# One file for ALL TE instances, regardless of exchange.
# Exchange credentials & bot config are managed via CMS, not here.
# =============================================================================

# === Database Server ===
DB_HOST=10.0.0.3
REDIS_PASSWORD=

# === Encryption ===
# For decrypting wallet credentials from MongoDB
# Generate with: openssl rand -hex 32
# MUST match CMS Backend key
ENCRYPTION_KEY=

# === Telegram Alerts ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Logging ===
LOG_LEVEL=info
RUST_LOG=info
# For debug: RUST_LOG=trading_engine=debug,tokio=warn
