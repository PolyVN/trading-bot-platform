# =============================================================================
# Trading Engine — OKX Host (Option A: Separate Host)
# Exchange credentials are managed via CMS API, NOT here.
# =============================================================================

# === Engine Identity ===
OKX_ENGINE_ID=te-okx-001

# === OKX Environment ===
OKX_ENVIRONMENT=live    # live | demo

# === Database Server ===
DB_HOST=10.0.0.3
REDIS_PASSWORD=         # Must match database .env

# === Encryption ===
ENCRYPTION_KEY=         # Generate: openssl rand -hex 32, must match CMS

# === Telegram Alerts ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Logging ===
LOG_LEVEL=info
RUST_LOG=info
