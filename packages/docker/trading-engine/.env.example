# =============================================================================
# Trading Engine — Per-host .env
# Same file for any exchange. ENGINE_ID and SUPPORTED_EXCHANGES vary per host.
# Exchange credentials are managed via CMS API, NOT here.
# =============================================================================

# === Engine Identity ===
POLYMARKET_ENGINE_ID=te-polymarket-001
# OKX_ENGINE_ID=te-okx-001              # Uncomment for Option B (co-located)

# === Database Server ===
DB_HOST=10.0.0.3
REDIS_PASSWORD=         # Must match database .env

# === Encryption ===
# For decrypting wallet credentials from MongoDB
# Generate: openssl rand -hex 32
# MUST match CMS Backend key
ENCRYPTION_KEY=

# === Telegram Alerts ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Logging ===
LOG_LEVEL=info
RUST_LOG=info
# Debug: RUST_LOG=trading_engine=debug,tokio=warn
