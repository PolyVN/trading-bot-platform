# Prometheus config — Trading Engine host (Option A/B)
# For Option C (strategy-isolated), use central prometheus on CMS host.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'te-polymarket'
    static_configs:
      - targets: ['te-polymarket:3010']
    metrics_path: /metrics

  # Uncomment for Option B (co-located OKX)
  # - job_name: 'te-okx'
  #   static_configs:
  #     - targets: ['te-okx:3010']
  #   metrics_path: /metrics
